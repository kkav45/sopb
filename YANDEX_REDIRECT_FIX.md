# 🔧 Исправление ошибки 404 - Redirect URI mismatch

## ❌ Ошибка

```
GET https://oauth.yandex.ru/authorize?...redirect_uri=http://localhost:8000 404 (Not Found)
redirect_uri не совпадает с Callback URL, указанным при регистрации приложения
```

---

## 🔍 Причина

**Redirect URI в коде** НЕ СОВПАДАЕТ с **Redirect URI в настройках Яндекс OAuth**.

Яндекс требует **точного совпадения** (символ в символ).

---

## ✅ Решение

### Шаг 1: Узнайте текущий Redirect URI в Яндексе

1. **Откройте:** https://oauth.yandex.ru/client/list
2. **Найдите** приложение "АСОПБ"
3. **Посмотрите** поле **Redirect URI**

Запомните, что там указано.

---

### Шаг 2: Приведите к соответствию

#### Вариант A: Обновить в Яндексе (рекомендуется)

1. Откройте https://oauth.yandex.ru/client/list
2. Найдите приложение "АСОПБ"
3. Нажмите **"Редактировать"**
4. В поле **Redirect URI** укажите:
   ```
   http://localhost:8000
   ```
5. Нажмите **"Сохранить"**

#### Вариант B: Обновить в коде

Если в Яндексе указан другой URI, обновите `js/yandex-disk.service.js`:

**Откройте файл** (строки 262-282):

```javascript
const YANDEX_CONFIG = {
  clientId: '52c92ec653874d10ac2a234e2ee7e8ea',
  clientSecret: '7ecec89a55344b7cb9b9c5df44d3fe5c',
  
  // Выберите ОДИН вариант (раскомментируйте):
  
  // 1. Для локальной разработки (простой):
  redirectUri: 'http://localhost:8000',
  
  // 2. Для локальной разработки (с callback файлом):
  // redirectUri: 'http://localhost:8000/yandex-auth-callback.html',
  
  // 3. Для GitHub Pages:
  // redirectUri: 'https://kkav45.github.io',
  
  // 4. Для GitHub Pages (с callback файлом):
  // redirectUri: 'https://kkav45.github.io/yandex-auth-callback.html',
  
  rootFolder: 'ASOPB'
};
```

**Раскомментируйте** нужный вариант, **закомментируйте** остальные.

---

### Шаг 3: Проверка

1. **Очистите кэш браузера:** Ctrl+Shift+R
2. **Откройте:** http://localhost:8000
3. **Нажмите:** "Подключить Яндекс.Диск"
4. **Разрешите доступ**

**Ожидаемый результат:**
- ✅ Перенаправит на `yandex-auth-callback.html`
- ✅ Появится сообщение "Яндекс.Диск подключён"
- ✅ Токен сохранится в localStorage

---

## 📊 Таблица соответствия Redirect URI

| В коде | В Яндекс OAuth | Статус |
|--------|---------------|--------|
| `http://localhost:8000` | `http://localhost:8000` | ✅ OK |
| `http://localhost:8000/yandex-auth-callback.html` | `http://localhost:8000/yandex-auth-callback.html` | ✅ OK |
| `https://kkav45.github.io` | `https://kkav45.github.io` | ✅ OK |
| `http://localhost:8000` | `http://localhost:8000/yandex-auth-callback.html` | ❌ ОШИБКА |
| `http://localhost:8000/yandex-auth-callback.html` | `http://localhost:8000` | ❌ ОШИБКА |

---

## 🔐 Проверка Client ID

Убедитесь, что Client ID правильный:

**В коде:** `52c92ec653874d10ac2a234e2ee7e8ea`  
**В Яндекс OAuth:** Должен совпадать

---

## 🌐 Для GitHub Pages

**Перед публикацией обновите:**

1. **В Яндексе:**
   ```
   Redirect URI: https://kkav45.github.io
   ```

2. **В коде** (`js/yandex-disk.service.js`):
   ```javascript
   redirectUri: 'https://kkav45.github.io',
   ```

3. **Загрузите изменения:**
   ```bash
   git add .
   git commit -m "Обновлён Redirect URI для GitHub Pages"
   git push
   ```

---

## 📝 Чек-лист

- [ ] Проверил Redirect URI в Яндекс OAuth
- [ ] Обновил Redirect URI в коде или в Яндексе
- [ ] Client ID совпадает
- [ ] Client Secret совпадает
- [ ] Очистил кэш браузера (Ctrl+Shift+R)
- [ ] Протестировал подключение

---

## 🆘 Если всё ещё ошибка

### 1. Проверьте URL авторизации

Откройте консоль (F12) → Network → Найдите запрос к `oauth.yandex.ru`

**URL должен выглядеть так:**
```
https://oauth.yandex.ru/authorize?
  response_type=token&
  client_id=52c92ec653874d10ac2a234e2ee7e8ea&
  redirect_uri=http://localhost:8000&
  scope=disk:app_folder
```

### 2. Пересоздайте приложение

Если ничего не помогает:

1. Удалите старое приложение: https://oauth.yandex.ru/client/list → Удалить
2. Создайте новое: https://oauth.yandex.ru/client/new
3. Укажите правильный Redirect URI: `http://localhost:8000`
4. Скопируйте новые Client ID и Client Secret
5. Обновите в `js/yandex-disk.service.js`

---

## 📞 Контакты поддержки

- **Яндекс OAuth Документация:** https://yandex.ru/dev/oauth/doc/ru/
- **Репозиторий проекта:** https://github.com/kkav45/sopb

---

**Дата исправления:** Февраль 2026  
**Версия:** 2.3  
**Статус:** ✅ Готово к использованию
