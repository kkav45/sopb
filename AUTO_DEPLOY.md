# 🔄 Автоматическая загрузка на GitHub

## ✅ Настроено для репозитория

**URL:** https://github.com/kkav45/sopb.git  
**Ветка:** main

---

## 📋 Доступные способы загрузки

### 1️⃣ GitHub Actions (АВТОМАТИЧЕСКИ) 🤖

**Работает автоматически при каждом push!**

При отправке изменений в ветку `main`:
1. GitHub автоматически запускает workflow
2. Сайт собирается и публикуется на GitHub Pages
3. Через 1-2 минуты доступен по адресу:
   ```
   https://kkav45.github.io/sopb/
   ```

**Статус деплоя:** https://github.com/kkav45/sopb/actions

---

### 2️⃣ PowerShell скрипт (Автоматически) ⚡

**Использование:**

```powershell
# Базовая загрузка
.\auto-deploy.ps1

# С сообщением
.\auto-deploy.ps1 -message "Добавлена новая функция"

# С произвольным сообщением
.\auto-deploy.ps1 -message "Исправление ошибок"
```

**Что делает скрипт:**
- ✅ Проверяет наличие Git
- ✅ Проверяет изменения
- ✅ Добавляет все файлы
- ✅ Создаёт коммит с сообщением
- ✅ Загружает на GitHub

---

### 3️⃣ Batch-скрипт (Windows) 🪟

**Использование:**

Просто дважды кликните на `deploy.bat`

Или в командной строке:
```batch
deploy.bat
```

---

### 4️⃣ Вручную (Git CLI) 📝

**Базовые команды:**

```bash
# Перейти в папку проекта
cd "d:\! Открытая платформа безопасности\!!!! Сайт\АСОПБ v 0.4 (моб)\asopb-html-prototype"

# Проверить статус
git status

# Добавить все файлы
git add .

# Создать коммит
git commit -m "Ваше сообщение"

# Загрузить на GitHub
git push
```

**Полезные команды:**

```bash
# Просмотр истории коммитов
git log --oneline

# Последние изменения
git diff

# Отмена коммита (до push)
git reset HEAD~1

# Синхронизация с GitHub
git pull origin main
```

---

## 🌐 Включение GitHub Pages

### Шаг 1: Откройте настройки репозитория

Перейдите на: https://github.com/kkav45/sopb/settings/pages

### Шаг 2: Настройте источник

1. **Build and deployment** → **Source**: Deploy from a branch
2. **Branch**: main / root
3. Нажмите **Save**

### Шаг 3: Дождитесь деплоя

- GitHub Actions автоматически запустит workflow
- Через 1-2 минуты сайт будет доступен
- URL появится в настройках Pages

---

## ⚙️ Настройка Git (если ещё не настроено)

### Первая настройка:

```bash
# Настройка имени и email
git config user.name "Ваше Имя"
git config user.email "your-email@example.com"

# Проверка настроек
git config --list
```

### Проверка подключения к репозиторию:

```bash
# Проверка remote URL
git remote -v

# Должно вывести:
# origin  https://github.com/kkav45/sopb.git (fetch)
# origin  https://github.com/kkav45/sopb.git (push)
```

---

## 🔐 Аутентификация на GitHub

### Personal Access Token (рекомендуется)

**Создание токена:**

1. https://github.com/settings/tokens
2. **Generate new token (classic)**
3. **Note:** `ASOPB Project`
4. **Expiration:** `No expiration` (или 90 дней)
5. **Scopes:** ✓ `repo` (полный доступ)
6. Нажмите **Generate token**
7. **Скопируйте токен** (показывается один раз!)

**Использование токена:**

При первом `git push` введите:
- **Username:** ваш ник GitHub
- **Password:** вставьте токен (не отобразится)

### Git Credential Manager

Для сохранения учётных данных:

```bash
# Очистить старые учётные данные
git credential-manager-core erase

# При следующем push введите новые
git push
```

---

## 📊 Статус и мониторинг

### Проверка статуса GitHub Actions:

1. https://github.com/kkav45/sopb/actions
2. Выберите последний запуск
3. Просмотрите логи деплоя

### Проверка статуса Pages:

1. https://github.com/kkav45/sopb/settings/pages
2. Прокрутите до **GitHub Pages site**
3. Проверьте статус деплоя

---

## 🚨 Решение проблем

### Ошибка: "Permission denied"

**Решение:**
```bash
# Проверьте права доступа к репозиторию
# Убедитесь, что вы владелец или имеете права на запись
```

### Ошибка: "Authentication failed"

**Решение:**
1. Создайте новый токен: https://github.com/settings/tokens
2. Обновите учётные данные:
   ```bash
   git credential-manager-core erase
   git push
   ```
3. Введите новый токен

### Ошибка: "Workflow not running"

**Причины:**
- Workflow файл повреждён
- Нет прав на Actions
- Репозиторий private (нужно включить Actions)

**Решение:**
1. Проверьте `.github/workflows/deploy.yml`
2. Включите Actions в настройках репозитория
3. Запустите вручную: **Actions** → **Deploy** → **Run workflow**

### Ошибка: "Large files"

Если файл больше 100MB:

```bash
# Добавьте в .gitignore большие файлы
echo "*.zip" >> .gitignore
echo "*.rar" >> .gitignore
echo "*.iso" >> .gitignore

# Удалите из индекса
git rm --cached *.zip
git commit -m "Remove large files"
git push
```

---

## 📅 Автоматизация по расписанию

### Планировщик заданий Windows

**Создание задачи:**

1. Откройте **Планировщик заданий**
2. **Создать задачу**
3. **Общие:**
   - Имя: `ASOPB Auto Deploy`
   - ✓ Запустить с наивысшими правами
4. **Триггеры:**
   - Создать триггер
   - Расписание (например, каждый день в 18:00)
5. **Действия:**
   - Создать действие
   - Программа: `powershell.exe`
   - Аргументы: `-File "D:\Путь\к\auto-deploy.ps1"`
   - Рабочая папка: `D:\Путь\к\asopb-html-prototype`
6. **Условия:**
   - ✓ Запускать только при наличии сетевого подключения
7. **Параметры:**
   - ✓ Останавливать по истечении 1 часа

---

## 💡 Советы по автоматизации

✅ **Коммитьте часто** — небольшие изменения легче отслеживать

✅ **Используйте понятные сообщения** — что и зачем изменили

✅ **Настройте pre-commit хуки** — для автоматической проверки кода

✅ **Используйте .gitignore** — не коммитьте временные файлы

✅ **Проверяйте перед push** — `git status` и `git diff`

✅ **Создавайте теги версий:**
```bash
git tag v0.5.0
git push origin v0.5.0
```

---

## 📚 Структура автоматизации

```
asopb-html-prototype/
├── .github/
│   └── workflows/
│       └── deploy.yml      # Авто-деплой на Pages
├── .gitignore              # Игнорируемые файлы
├── auto-deploy.ps1         # PowerShell скрипт
├── deploy.bat              # Batch скрипт
├── setup-git.bat           # Первая настройка
└── AUTO_DEPLOY.md          # Эта инструкция
```

---

## 🔗 Полезные ссылки

- **Репозиторий:** https://github.com/kkav45/sopb
- **Actions:** https://github.com/kkav45/sopb/actions
- **Pages:** https://github.com/kkav45/sopb/settings/pages
- **Tokens:** https://github.com/settings/tokens
- **Git Docs:** https://git-scm.com/doc

---

## 📞 Быстрая справка

| Задача | Команда |
|--------|---------|
| **Загрузить изменения** | `.\auto-deploy.ps1` |
| **Проверить статус** | `git status` |
| **Посмотреть историю** | `git log --oneline` |
| **Отменить коммит** | `git reset HEAD~1` |
| **Синхронизироваться** | `git pull` |
| **Создать тег** | `git tag v0.5.0` |

---

**Версия:** 1.0  
**Дата:** Февраль 2026  
**Репозиторий:** https://github.com/kkav45/sopb.git
