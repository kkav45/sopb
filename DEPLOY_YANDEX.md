# 🚀 Развёртывание на GitHub Pages

## 📦 Файлы для развёртывания

Для работы с Яндекс.Диском на GitHub Pages необходимо добавить следующие файлы в репозиторий:

### 1. `yandex-auth-callback.html`

Этот файл должен находиться в **корне репозитория** (рядом с `index.html`).

**Путь:** `sopb/yandex-auth-callback.html`

Файл уже находится в папке `asopb-html-prototype/`. Скопируйте его в корень репозитория:

```bash
# Из корня репозитория sopb
copy asopb-html-prototype/yandex-auth-callback.html .
```

### 2. `js/yandex-disk.service.js`

Сервис для работы с Яндекс.Диском.

**Путь:** `sopb/js/yandex-disk.service.js`

Убедитесь, что файл содержит правильную конфигурацию:

```javascript
const YANDEX_CONFIG = {
  clientId: '4e7a93423bf645aaa0f82ab32f8b8f9f',
  clientSecret: '1e162828461949d89dbf21ebd8d061ab',
  redirectUri: 'https://kkav45.github.io/sopb/yandex-auth-callback.html',
  rootFolder: 'ASOPB'
};
```

---

## 📝 Пошаговая инструкция

### Шаг 1: Проверка OAuth приложения

1. Перейдите в [Яндекс OAuth](https://oauth.yandex.ru/clients)
2. Найдите приложение с Client ID: `4e7a93423bf645aaa0f82ab32f8b8f9f`
3. Проверьте **Redirect URI**:
   ```
   https://kkav45.github.io/sopb/yandex-auth-callback.html
   ```
4. Проверьте разрешения:
   - ✅ `disk:app_folder` — Доступ к папке приложения

### Шаг 2: Добавление файлов в репозиторий

```bash
# Перейдите в корень репозитория
cd sopb

# Скопируйте yandex-auth-callback.html в корень
copy asopb-html-prototype\yandex-auth-callback.html yandex-auth-callback.html

# Скопируйте обновлённые JS файлы
copy asopb-html-prototype\js\yandex-disk.service.js js\yandex-disk.service.js
copy asopb-html-prototype\js\sync-manager.service.js js\sync-manager.service.js
copy asopb-html-prototype\js\app.js js\app.js
copy asopb-html-prototype\js\main.js js\main.js
copy asopb-html-prototype\js\utils.js js\utils.js
copy asopb-html-prototype\js\local-cache.service.js js\local-cache.service.js

# Добавьте изменения в Git
git add yandex-auth-callback.html
git add js/yandex-disk.service.js
git add js/sync-manager.service.js
git add js/app.js
git add js/main.js
git add js/utils.js
git add js/local-cache.service.js

# Сделайте коммит
git commit -m "Добавлена интеграция с Яндекс.Диском"

# Отправьте на GitHub
git push origin main
```

### Шаг 3: Обновление на GitHub Pages

После push в ветку `main`, GitHub Pages автоматически обновится в течение 1-2 минут.

Проверьте:
- https://kkav45.github.io/sopb/
- https://kkav45.github.io/sopb/yandex-auth-callback.html

---

## ✅ Проверка работы

### 1. Проверка доступности файлов

Откройте в браузере:

```
https://kkav45.github.io/sopb/yandex-auth-callback.html
```

Должна загрузиться страница с заголовком **"Яндекс.Диск - Авторизация"**.

### 2. Проверка подключения

1. Откройте https://kkav45.github.io/sopb/
2. Перейдите в **Настройки** (⚙️)
3. Нажмите **"🔗 Подключить Яндекс.Диск"**
4. Разрешите приложению доступ
5. Проверьте статус: **"✓ Подключено"**

---

## 🔧 Обновление файлов в будущем

При внесении изменений в локальную версию `asopb-html-prototype`:

```bash
# Синхронизация изменений с репозиторием
robocopy asopb-html-prototype js js\*.js /XF sw.js
robocopy asopb-html-prototype . yandex-auth-callback.html

git add .
git commit -m "Обновление интеграции Яндекс.Диска"
git push origin main
```

---

## 🐛 Устранение неполадок

### Ошибка 404 для yandex-auth-callback.html

**Проблема:** Файл не найден на GitHub Pages

**Решение:**
1. Убедитесь, что файл находится в **корне репозитория**
2. Проверьте, что файл закоммичен и отправлен на GitHub
3. Подождите 1-2 минуты для обновления GitHub Pages

### Ошибка OAuth

**Проблема:** "Redirect URI mismatch"

**Решение:**
1. Проверьте Redirect URI в настройках OAuth приложения
2. Убедитесь, что он точно совпадает с:
   ```
   https://kkav45.github.io/sopb/yandex-auth-callback.html
   ```
3. Проверьте регистр букв (URL чувствителен к регистру)

### Токен не сохраняется

**Проблема:** После авторизации токен не сохраняется

**Решение:**
1. Проверьте консоль браузера (F12) на ошибки
2. Убедитесь, что localStorage доступен
3. Проверьте, что `yandex-auth-callback.html` загружается правильно

---

## 📊 Структура репозитория

После развёртывания структура должна быть такой:

```
sopb/
├── .github/
├── js/
│   ├── app.js
│   ├── main.js
│   ├── utils.js
│   ├── local-cache.service.js
│   ├── sync-manager.service.js
│   ├── yandex-disk.service.js  ← Новый файл
│   └── ...
├── styles/
│   └── main.css
├── index.html
├── yandex-auth-callback.html   ← Новый файл
├── manifest.json
├── sw.js
└── README.md
```

---

## 📞 Поддержка

При возникновении проблем:
1. Проверьте консоль браузера (F12 → Console)
2. Проверьте Network tab для OAuth запросов
3. Убедитесь, что OAuth приложение настроено правильно
4. Проверьте, что файлы доступны по прямым ссылкам GitHub Pages
