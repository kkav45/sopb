# 📝 Инструкция по подключению Яндекс.Диска

## 🎯 Возможности

После подключения Яндекс.Диска:
- **Автосохранение** всех данных (объекты, оборудование, проверки, нарушения)
- **Синхронизация** между устройствами
- **Резервное копирование** в облако
- **Версионность** данных

---

## 📋 Шаг 1: Настройка OAuth приложения в Яндексе

### 1.1 Регистрация приложения

1. Перейдите в [Яндекс OAuth](https://oauth.yandex.ru/client/new)
2. Заполните данные приложения:

   **Название:**
   ```
   АСОПБ — Система пожарной безопасности
   ```

   **Платформы:**
   - ✅ Веб-сервисы

   **Redirect URI:**
   ```
   https://kkav45.github.io/sopb/yandex-auth-callback.html
   ```

   **Разрешения (OAuth):**
   - ✅ `disk:app_folder` — Доступ к папке приложения на Яндекс.Диске

3. Нажмите **«Сохранить»**

### 1.2 Получение Client ID

После сохранения вы получите:
- **Client ID** (например: `4e7a93423bf645aaa0f82ab32f8b8f9f`)
- **Client Secret** (например: `1e162828461949d89dbf21ebd8d061ab`)

---

## 🔧 Шаг 2: Настройка приложения

### 2.1 Обновление конфигурации

Откройте файл `js/yandex-disk.service.js` и проверьте конфигурацию:

```javascript
const YANDEX_CONFIG = {
  clientId: 'ВАШ_CLIENT_ID',
  clientSecret: 'ВАШ_CLIENT_SECRET',
  redirectUri: 'https://kkav45.github.io/sopb/yandex-auth-callback.html',
  rootFolder: 'ASOPB'
};
```

### 2.2 Проверка redirectUri

**Важно!** Redirect URI в конфигурации должен **ТОЧНО** совпадать с указанным при регистрации OAuth приложения.

---

## 🚀 Шаг 3: Подключение Яндекс.Диска

### 3.1 Запуск приложения

1. Откройте приложение в браузере
2. Перейдите в раздел **Настройки** (⚙️ в боковой панели)

### 3.2 Авторизация

1. В секции **«Яндекс.Диск»** нажмите кнопку **«🔗 Подключить Яндекс.Диск»**
2. Откроется окно авторизации Яндекса
3. Разрешите приложению доступ к Яндекс.Диску
4. После успешной авторизации окно закроется автоматически

### 3.3 Проверка подключения

После подключения:
- Статус изменится на **«✓ Подключено»**
- Появится информация о последней синхронизации
- В боковой панели статус синхронизации станет зелёным

---

## 📁 Структура папок на Яндекс.Диске

После подключения на вашем Яндекс.Диске будет создана папка:

```
ASOPB/
├── objects/          # Объекты
│   └── obj-xxx.json
├── equipment/        # Оборудование
│   └── eq-xxx.json
├── inspections/      # Проверки
│   └── insp-xxx.json
├── violations/       # Нарушения
│   └── viol-xxx.json
└── metadata/         # Служебные данные
    └── sync-status.json
```

---

## 🔄 Синхронизация

### Автоматическая синхронизация

- При изменении данных они **автоматически** добавляются в очередь синхронизации
- Синхронизация происходит при нажатии кнопки **🔄 Синхронизировать**
- Частота автосинхронизации: каждые 30 секунд (при подключении)

### Ручная синхронизация

Нажмите кнопку **🔄** в верхней панели для принудительной синхронизации.

### Статусы синхронизации

| Статус | Значение |
|--------|----------|
| 🔄 Синхронизация... | Данные загружаются на диск |
| ✓ Синхронизировано | Все данные актуальны |
| 💾 Локально | Работа без подключения к облаку |
| ⚠️ Ошибка | Проблема при синхронизации |

---

## 🔐 Безопасность

### Хранение токена

- Токен сохраняется в `localStorage` браузера
- Срок действия токена: **1 год** (Implicit Flow)
- Токен не передаётся третьим сторонам

### Разрешения

Приложение запрашивает минимальные разрешения:
- `disk:app_folder` — доступ **только** к папке `ASOPB/` на вашем Диске
- **Нет доступа** к другим файлам на Яндекс.Диске

---

## ❓ Устранение неполадок

### Ошибка «Не подключён»

**Причина:** Токен истёк или не был получен

**Решение:**
1. Нажмите кнопку **«🚫 Отключиться»**
2. Подключитесь заново через **«🔗 Подключить Яндекс.Диск»**

### Ошибка синхронизации

**Причина:** Нет подключения к интернету или проблемы с API Яндекса

**Решение:**
1. Проверьте подключение к интернету
2. Попробуйте синхронизировать повторно
3. Если ошибка повторяется — отключитесь и подключитесь заново

### Данные не синхронизируются

**Причина:** Конфликт версий или квота диска превышена

**Решение:**
1. Проверьте свободное место на Яндекс.Диске
2. Очистите очередь синхронизации в консоли разработчика:
   ```javascript
   localStorage.clear()
   ```
3. Выполните полную синхронизацию

---

## 📊 Мониторинг

### Просмотр статуса

1. **Боковая панель** — быстрый статус подключения
2. **Настройки → Яндекс.Диск** — подробная информация:
   - Статус подключения
   - Время последней синхронизации
   - Папка на диске

### Логи синхронизации

Откройте консоль разработчика (F12) для просмотра детальных логов:
- Успешные синхронизации
- Ошибки загрузки/выгрузки
- Конфликты версий

---

## 📞 Поддержка

При возникновении проблем:
1. Проверьте консоль разработчика (F12) на наличие ошибок
2. Убедитесь, что Redirect URI совпадает
3. Проверьте, что OAuth приложение имеет разрешение `disk:app_folder`
4. Переподключите Яндекс.Диск

---

## 📝 Примечания

- Для работы требуется стабильное подключение к интернету
- Первое подключение может занять до 1 минуты
- При работе офлайн данные сохраняются локально
- После появления интернета выполните синхронизацию вручную
