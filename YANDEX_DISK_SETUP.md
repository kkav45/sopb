# 🔐 Настройка интеграции с Яндекс.Диском

## 📋 Шаг 1: Регистрация OAuth приложения в Яндексе

### 1.1 Откройте Яндекс OAuth

Перейдите на: https://oauth.yandex.ru/client/new

### 1.2 Заполните данные приложения

**Основная информация:**

| Поле | Значение |
|------|----------|
| **Название приложения** | `АСОПБ Прототип` |
| **Контактный email** | ваш-email@example.com |
| **Платформа** | ☑️ Веб-сервисы |

**Redirect URI:**

```
http://localhost:8000
```

Или если используете GitHub Pages:
```
https://kkav45.github.io
```

**Разрешения (Scopes):**

✅ **disk:app_folder** — Доступ к папке приложения  
✅ **disk:app_folder:readonly** — Только чтение (опционально)

**Нажмите "Сохранить"**

### 1.3 Сохраните учётные данные

После сохранения вы получите:

- **ID приложения (Client ID):** `xxxxxxxxxxxxxxxxxxxx`
- **Секретный ключ (Client Secret):** `yyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyy`

**⚠️ Важно:** Сохраните Client Secret — он показывается только один раз!

---

## 📋 Шаг 2: Настройка в проекте

### 2.1 Откройте файл конфигурации

Файл: `js/yandex-disk.service.js`

### 2.2 Замените конфигурацию

Найдите строки 139-144:

```javascript
const YANDEX_CONFIG = {
  clientId: 'YOUR_CLIENT_ID',
  clientSecret: 'YOUR_CLIENT_SECRET',
  redirectUri: window.location.origin,
  rootFolder: 'ASOPB'
};
```

Замените на ваши данные:

```javascript
const YANDEX_CONFIG = {
  clientId: 'xxxxxxxxxxxxxxxxxxxx',           // Ваш Client ID
  clientSecret: 'yyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyy',  // Ваш Client Secret
  redirectUri: 'http://localhost:8000',       // Ваш Redirect URI
  rootFolder: 'ASOPB'                         // Папка на Яндекс.Диске
};
```

### 2.3 Сохраните файл

---

## 📋 Шаг 3: Первая авторизация

### 3.1 Откройте приложение

```
http://localhost:8000
```

### 3.2 Нажмите "Подключить Яндекс.Диск"

На дашборде в секции "Яндекс.Диск" нажмите кнопку **"Подключить Яндекс.Диск"**

### 3.3 Разрешите доступ

1. Откроется страница авторизации Яндекса
2. Нажмите **"Разрешить"** для предоставления доступа
3. Вас перенаправит обратно на приложение

### 3.4 Проверка подключения

Если всё успешно:
- ✅ Статус изменится на **"Подключено"**
- ✅ Появится имя пользователя
- ✅ Кнопка "Синхронизировать" станет активной

---

## 📋 Шаг 4: Проверка работы

### 4.1 Создайте тестовый объект

1. Перейдите в **"Объекты"**
2. Нажмите **"+ Добавить объект"**
3. Заполните форму:
   - Название: `Тестовый объект`
   - Адрес: `г. Тестовск, ул. Примерная, д. 1`
   - Класс ФПО: `Ф4.3`
4. Нажмите **"Сохранить"**

### 4.2 Проверьте синхронизацию

1. Нажмите кнопку **"🔄 Синхронизировать"** в верхней панели
2. Должно появиться сообщение: **"Синхронизация завершена: 1 файлов загружено"**

### 4.3 Проверьте Яндекс.Диск

1. Откройте https://disk.yandex.ru
2. Перейдите в папку **Приложения**
3. Найдите папку **ASOPB**
4. Внутри должны быть папки:
   - `objects/`
   - `equipment/`
   - `metadata/`

В папке `objects/` должен быть файл с вашим объектом:
```
obj-{uuid}.json
```

---

## 🔧 Решение проблем

### Ошибка: "Redirect URI mismatch"

**Причина:** Redirect URI в настройках приложения не совпадает с фактическим

**Решение:**
1. Откройте https://oauth.yandex.ru/client/new
2. Найдите ваше приложение
3. Измените Redirect URI на правильный:
   - Для локальной разработки: `http://localhost:8000`
   - Для GitHub Pages: `https://kkav45.github.io`
4. Сохраните изменения

### Ошибка: "Invalid client_id"

**Причина:** Неверный Client ID

**Решение:**
1. Проверьте Client ID в настройках приложения
2. Убедитесь, что он скопирован без пробелов
3. Обновите в файле `js/yandex-disk.service.js`

### Ошибка: "Access denied" или "Token expired"

**Причина:** Истёк срок действия токена

**Решение:**
```javascript
// В консоли браузера (F12) выполните:
localStorage.removeItem('yandex-disk-token');
location.reload();
```

Затем подключите Яндекс.Диск заново.

### Ошибка: "CORS policy"

**Причина:** Браузер блокирует запросы из-за политики безопасности

**Решение:**
1. Убедитесь, что используете HTTP-сервер (не file://)
2. Запустите через Python:
   ```bash
   python -m http.server 8000
   ```
3. Или через Node.js:
   ```bash
   npx http-server -p 8000
   ```

### Ошибка: "App folder not found"

**Причина:** Папка приложения ещё не создана

**Решение:**
1. Откройте https://disk.yandex.ru
2. Перейдите в **Приложения**
3. Найдите приложение "АСОПБ Прототип"
4. Нажмите на него — папка создастся автоматически

---

## 🔐 Безопасность

### ⚠️ Никогда не публикуйте Client Secret!

**Добавьте в .gitignore:**

```gitignore
# Конфигурация с секретами
js/yandex-disk.service.js.local
*.secret
*.key
```

### Используйте .env файл (рекомендуется)

Создайте файл `.env` в корне проекта:

```env
YANDEX_CLIENT_ID=xxxxxxxxxxxxxxxxxxxx
YANDEX_CLIENT_SECRET=yyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyy
YANDEX_REDIRECT_URI=http://localhost:8000
```

**Обновите .gitignore:**

```gitignore
# Переменные окружения
.env
.env.local
.env.*.local
```

**Обновите js/yandex-disk.service.js:**

```javascript
const YANDEX_CONFIG = {
  clientId: process.env.YANDEX_CLIENT_ID || 'YOUR_CLIENT_ID',
  clientSecret: process.env.YANDEX_CLIENT_SECRET || 'YOUR_CLIENT_SECRET',
  redirectUri: process.env.YANDEX_REDIRECT_URI || window.location.origin,
  rootFolder: 'ASOPB'
};
```

---

## 📊 Структура данных на Яндекс.Диске

После подключения создаётся следующая структура:

```
ASOPB/
├── objects/
│   └── obj-{uuid}.json          # Карточки объектов
├── equipment/
│   └── eq-{uuid}.json           # Карточки оборудования
├── inspections/
│   └── {year}/
│       └── {month}/
│           └── insp-{uuid}.json # Результаты проверок
├── violations/
│   └── viol-{uuid}.json         # Нарушения
├── documents/
│   ├── acts/                    # Акты проверок
│   ├── journals/                # Журналы
│   └── protocols/               # Протоколы
└── metadata/
    └── sync-status.json         # Статус синхронизации
```

---

## 🔄 Автоматическая синхронизация

### Настройка авто-синхронизации

В файле `js/app.js` найдите строку ~60:

```javascript
// Запуск авто-синхронизации
this.startAutoSync();
```

**Параметры:**

```javascript
// Запуск авто-синхронизации каждые 30 секунд
this.startAutoSync(30000);

// Или отключить авто-синхронизацию
// this.startAutoSync(); // закомментировать
```

### Ручная синхронизация

Нажмите кнопку **"🔄 Синхронизировать"** в верхней панели

Или в консоли:
```javascript
await window.app.syncManager.sync();
```

---

## 📝 Логи и отладка

### Включение логирования

В консоли браузера (F12) выполните:

```javascript
// Включить подробные логи
localStorage.setItem('asopb_debug', 'true');
location.reload();
```

### Просмотр статуса синхронизации

```javascript
// Текущий статус
console.log('Статус:', await window.app.yandexDisk.isAuthenticated());

// Информация о токене
console.log('Токен:', window.app.yandexDisk.getTokenInfo());

// Статистика кэша
console.log('Кэш:', await window.app.localCache.getStats());

// Принудительная синхронизация
await window.app.syncManager.sync();
```

### Очистка кэша

```javascript
// Очистить весь кэш
await window.app.localCache.clear();
location.reload();
```

---

## 🌐 Публикация на GitHub Pages

### Обновите Redirect URI

В настройках OAuth приложения на Яндексе:

```
https://kkav45.github.io
```

### Обновите конфигурацию

В файле `js/yandex-disk.service.js`:

```javascript
const YANDEX_CONFIG = {
  clientId: 'YOUR_CLIENT_ID',
  clientSecret: 'YOUR_CLIENT_SECRET',
  redirectUri: 'https://kkav45.github.io',  // Изменено
  rootFolder: 'ASOPB'
};
```

### Загрузите изменения

```bash
git add .
git commit -m "Настройка Яндекс.Диска для GitHub Pages"
git push
```

---

## 📚 API Яндекс.Диска

### Основные методы

```javascript
// Авторизация
const authUrl = yandexDisk.getAuthUrl();
await yandexDisk.exchangeCodeForToken(code);
await yandexDisk.isAuthenticated();

// Файлы
await yandexDisk.readFile('objects/test.json');
await yandexDisk.writeFile('objects/test.json', data);
await yandexDisk.deleteFile('objects/test.json');
await yandexDisk.listFiles('objects');
await yandexDisk.fileExists('objects/test.json');

// Утилиты
await yandexDisk.disconnect();
const tokenInfo = yandexDisk.getTokenInfo();
```

### Пример использования

```javascript
// Чтение файла
try {
  const data = await window.app.yandexDisk.readFile('objects/obj-001.json');
  console.log('Данные объекта:', data);
} catch (error) {
  console.error('Ошибка чтения:', error);
}

// Запись файла
try {
  const objectData = {
    id: 'obj-001',
    name: 'Тестовый объект',
    address: 'г. Москва'
  };
  await window.app.yandexDisk.writeFile('objects/obj-001.json', objectData);
  console.log('Файл записан');
} catch (error) {
  console.error('Ошибка записи:', error);
}
```

---

## 🔗 Полезные ссылки

- **OAuth приложение:** https://oauth.yandex.ru/client/new
- **Мои приложения:** https://oauth.yandex.ru/client/list
- **Яндекс.Диск:** https://disk.yandex.ru
- **API Документация:** https://yandex.ru/dev/disk-api/doc/ru/
- **OAuth Документация:** https://yandex.ru/dev/oauth/doc/ru/

---

## ✅ Чек-лист настройки

- [ ] Зарегистрировано OAuth приложение в Яндексе
- [ ] Получены Client ID и Client Secret
- [ ] Настроен Redirect URI
- [ ] Обновлён файл `js/yandex-disk.service.js`
- [ ] Разрешён доступ приложению
- [ ] Статус "Подключено" отображается
- [ ] Синхронизация работает
- [ ] Файлы создаются на Яндекс.Диске

---

**Версия:** 1.0  
**Дата:** Февраль 2026  
**Статус:** Готово к использованию
